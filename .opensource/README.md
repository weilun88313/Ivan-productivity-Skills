# 开源同步系统

此目录包含将私有仓库同步到公开仓库的自动化工具。

## 📁 文件说明

- **sync-to-public.sh** - 自动同步脚本
- **brand-config.json** - 品牌信息替换配置
- **README.md** - 本文件

## 🚀 快速开始

### 首次设置

```bash
# 1. 给脚本添加执行权限
chmod +x .opensource/sync-to-public.sh

# 2. 在GitHub创建公开仓库
# 访问 https://github.com/new
# 仓库名建议: Ivan_Skills_Public 或 Claude-Code-Skills

# 3. 克隆并设置公开仓库
cd ~/Documents
git clone /Users/ivan/Documents/Ivan_Skills Ivan_Skills_Public
cd Ivan_Skills_Public
git remote rename origin private
git remote add origin git@github.com:YOUR_USERNAME/Ivan_Skills_Public.git

# 4. 首次同步
cd /Users/ivan/Documents/Ivan_Skills
./.opensource/sync-to-public.sh "Initial open source release"

# 5. 推送到公开仓库
cd ~/Documents/Ivan_Skills_Public
git push origin main -u
```

### 日常使用

```bash
# 在私有仓库正常工作
cd /Users/ivan/Documents/Ivan_Skills
# ... 编辑代码，添加新功能 ...
git add .
git commit -m "Add new feature"
git push

# 同步到公开仓库（一键操作）
./.opensource/sync-to-public.sh "Add new feature"

# 推送到公开仓库
cd ~/Documents/Ivan_Skills_Public
git push origin main
```

## 🔄 工作流程

```
你的日常工作流程：
┌─────────────────────────────────────────────┐
│  1. 在 Ivan_Skills (私有) 正常开发          │
│     - 使用真实品牌名 Lensmor                 │
│     - 使用真实作者信息                       │
│     - 提交到私有仓库                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  2. 运行同步脚本                             │
│     ./.opensource/sync-to-public.sh         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  3. 脚本自动执行：                           │
│     ✓ 拉取最新代码到 Ivan_Skills_Public      │
│     ✓ 替换 Lensmor → MyCompany              │
│     ✓ 替换作者信息为示例                     │
│     ✓ 更新许可证为 MIT                       │
│     ✓ 自动提交变更                           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  4. 检查并推送到公开仓库                     │
│     cd ~/Documents/Ivan_Skills_Public       │
│     git push origin main                    │
└─────────────────────────────────────────────┘
```

## 📝 自动清理的内容

脚本会自动执行以下清理操作：

### 1. 品牌名称替换
- `Lensmor` → `MyCompany`
- `lensmor` → `mycompany`
- `.env` (unchanged)

### 2. 作者信息匿名化
- 真实姓名和头像 → 示例占位符
- 位置：`Skill/webflow-blog-publisher/assets/writers/writers.json`

### 3. 许可证更新
- 私有项目声明 → MIT开源许可证

### 4. 排除私有内容
- `Skill/brand-guidelines/` - 包含公司特定品牌信息
- `Skill/*/output/` - 所有Skills的输出目录
- 这些目录完全不会出现在公开仓库中

### 5. 添加开源文件
- `CONTRIBUTING.md` - 贡献指南
- `LICENSE` - MIT许可证

## 🔧 自定义配置

编辑 `brand-config.json` 可以添加更多替换规则：

```json
{
  "replacements": {
    "brand_name": {
      "private": "Lensmor",
      "public": "MyCompany"
    }
  }
}
```

## ⚠️ 注意事项

1. **始终在私有仓库开发** - 公开仓库只用于同步，不要直接修改
2. **检查后再推送** - 运行脚本后检查 `Ivan_Skills_Public` 的变更
3. **敏感信息** - 确保 `.gitignore` 正确配置，不要提交密钥
4. **选择性同步** - 如果某个commit包含敏感信息，可以跳过不同步

## 🛡️ 安全检查清单

同步前检查：
- [ ] workspace/ 目录已被 .gitignore 忽略
- [ ] 没有硬编码的API密钥
- [ ] 没有包含客户信息的示例文件
- [ ] Git历史中没有敏感数据

## 📚 FAQ

### Q: 如果我只想同步某些commits怎么办？
A: 使用 `git cherry-pick` 手动选择：
```bash
cd ~/Documents/Ivan_Skills_Public
git cherry-pick <commit-hash>
# 然后运行清理部分的sed命令
```

### Q: 如何回滚公开仓库的某个版本？
A:
```bash
cd ~/Documents/Ivan_Skills_Public
git revert <commit-hash>
git push origin main
```

### Q: 脚本会修改私有仓库吗？
A: **不会**。脚本只修改 `Ivan_Skills_Public`，私有仓库保持不变。

## 🎯 最佳实践

1. **定期同步** - 建议每周或每次重大更新后同步一次
2. **提交粒度** - 私有仓库的commits可以包含品牌信息，同步时会自动清理
3. **分支管理** - 公开仓库只维护 main 分支即可，保持简单
